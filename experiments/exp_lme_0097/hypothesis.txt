Jost+sigmoid decay with HIGHER lambda + retrieval_consolidation + additive:
- lambda_fact=0.012 (vs default 0.009), lambda_episode=0.060 (vs 0.040)
- Faster decay clears unused memories faster — tests if this helps precision
